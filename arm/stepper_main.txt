/* Private variables ---------------------------------------------------------*/

/* USER CODE BEGIN PV */

uint8_t rxData[6]; // 데이터 입력 받는거. "000 0\n" 형식으로 입력을 받으므로 6자리 배정
uint8_t flag = 0; // flag 설정
char pData[] = "Your input should be 000 0"; // 주의 사항 출력용

/* USER CODE END PV */

/* Private function prototypes -----------------------------------------------*/
void SystemClock_Config(void);
/* USER CODE BEGIN PFP */

void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
  HAL_UART_Receive_DMA(&huart2, rxData, sizeof(rxData));
  flag++; // flag에 1을 더하여 실행되도록 함
}

/* USER CODE END PFP */

  /* USER CODE BEGIN 2 */
  HAL_UART_Receive_DMA(&huart2, rxData, sizeof(rxData));
  HAL_UART_Transmit_DMA(&huart2, pData, sizeof(pData)); // 주의사항 출력인데 왜인지 두 번 나옴
  // 시계 방향 180도
//  rotateDegrees(180, 0);

  /* USER CODE END 2 */

  /* Infinite loop */
  /* USER CODE BEGIN WHILE */
  while (1)
  {
    if(flag != 0)
    {
      rotateDegrees((rxData[0]-48)*100+(rxData[1]-48)*10+(rxData[2]-48), rxData[4]-48);
      // uint8_t 배열은 입력을 문자열로 받음. 아스키 코드 상 숫자는 자신의 48을 더한 값에 배정되어 있음(예: 0은 48, 5는 53)
      // 또한 연속해서 입력 받을 경우 한자리에는 하나의 문자만 들어감. 그러므로 입력 형식을 정해서 입력을 받기로 함
      // 이때 "000 0" 형식으로 받기로 함. 앞의 3문자는 48씩 빼고 100, 10, 1을 곱하여 더함으로써 각도를 완성
      // 중간 띄어쓰기는 무시하고 배열상 4번은 48만 빼서 방향에 입력
      flag = 0; // 동작을 완료하면 flag를 0으로 만들어 동작 종료 및 연속 동작 방지
    }

    /* USER CODE END WHILE */

    /* USER CODE BEGIN 3 */
  }
  /* USER CODE END 3 */
}
