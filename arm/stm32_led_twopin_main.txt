int main(void)
{

  /* USER CODE BEGIN 1 */

  /* USER CODE END 1 */

  /* MCU Configuration--------------------------------------------------------*/

  /* Reset of all peripherals, Initializes the Flash interface and the Systick. */
  HAL_Init();

  /* USER CODE BEGIN Init */

  /* USER CODE END Init */

  /* Configure the system clock */
  SystemClock_Config();

  /* USER CODE BEGIN SysInit */

  /* USER CODE END SysInit */

  /* Initialize all configured peripherals */
  MX_GPIO_Init();
  /* USER CODE BEGIN 2 */

  LED_CONTROL led1 = {GPIOC, GPIO_PIN_8, 500, 0};
  LED_CONTROL led2 = {GPIOC, GPIO_PIN_6, 1000, 0}; // 두번째 포트 PC6



  /* USER CODE END 2 */

  /* Infinite loop */
  /* USER CODE BEGIN WHILE */
  while (1)
  {

    updateLed(&led1); // 0.5초 간격으로 깜빡임
    updateLed(&led2); // 1초 간격으로 깜빡임
		// 두개를 다룰 때는 위와 같은 방법이 유리
	
    // HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_8);
    // HAL_Delay(500);
    // HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_9);
    // HAL_Delay(1000);
    // 위와 같이 하면 둘다 1.5초마다 깜빡이게 됨
    /* USER CODE END WHILE */

    /* USER CODE BEGIN 3 */
  }
  /* USER CODE END 3 */
}
