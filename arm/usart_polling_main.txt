uint8_t rxData[10];
  while (1)
  {
    HAL_UART_Receive(&huart2, rxData, 4, 1000); // huart2는 주소. usart.c에서 확인 가능. 데이터는 배열로 선언했기에 &안써도 됨.
    // 사이즈는 4로 설정, timeout는 100ms
    HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_8);
    HAL_Delay(200);
    /* USER CODE END WHILE */

    /* USER CODE BEGIN 3 */
  }
  /* USER CODE END 3 */
}
